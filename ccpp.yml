name: C/C++ CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/main
    - name: clone
      run: git submodule update --init
    - name: build
      run: |
        mkdir build
        cd build
        cmake ..
        make

